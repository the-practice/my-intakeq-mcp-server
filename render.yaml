services:
  - type: web
    name: intakeq-mcp-server
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 8000
    scaling:
      minInstances: 1
      maxInstances: 3
